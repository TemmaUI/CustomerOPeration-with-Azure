
@{
    ViewData["Title"] = "AuthorizedPage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>AuthorizedPage</h1>

@if(User.Identity.IsAuthenticated)
{
    @foreach (var claim in User.Claims)
        {
        <tr><td>@claim.Type</td><td>@claim.Value</td></tr>
        }
}

